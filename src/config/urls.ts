/** URL del portal de clientes (recarga de tarjetas). */
export const clientPortalUrl =
  import.meta.env.VITE_CLIENT_PORTAL_URL ?? 'https://portal.pesalink.com.ar';

/** Sitio corporativo (esta SPA en producción). */
export const corporateSiteUrl =
  import.meta.env.VITE_SITE_URL ?? 'https://www.pesalink.com.ar';
